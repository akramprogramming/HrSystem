<?php ini_set('display_errors', '1'); ini_set('display_startup_errors', '1'); error_reporting(E_ALL); require_once __DIR__ . '/../config/config.php'; require_once __DIR__ . '/../core/Database.php'; try { $db = \Core\Database::getConnection(); $val = $db->query("SELECT 1")->fetchColumn(); echo "DB OK — SELECT 1 => " . htmlspecialchars((string)$val); } catch (Throwable $e) { echo "DB ERROR: " . htmlspecialchars($e->getMessage()); } - افتح الرابط: https://followup.gt.tc/admin/debug_db.php - انسخ الناتج وأرسله هنا (مثلاً: DB OK — SELECT 1 => 1 أو رسالة خطأ).
